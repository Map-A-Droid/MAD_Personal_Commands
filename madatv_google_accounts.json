{
  "ATV - Get Google Accounts": [
    {
      "TYPE": "jobType.PASSTHROUGH",
      "SYNTAX": "awk '/com.android.contacts/ { if (match($0, /account=\"[^\"]+\"/)) print(substr($0, RSTART + 9, RLENGTH - 10)) }' /data/system/sync/accounts.xml"
    }
  ]
}
